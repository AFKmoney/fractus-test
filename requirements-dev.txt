# Pinned versions for dev (reproducibility).
# PyTorch CPU-only wheel on Windows:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

torch>=2.2,<3.0
numpy>=1.26,<3.0
pytest>=8.0,<9.0
maturin>=1.4,<2.0
